# llm-rag-lab — 개발 기록

## ① 앱의 목적과 대상

**텍스트 분석 → LLM → RAG → 에이전트** 를 브라우저에서 직접 돌려 보며 익히는 학습 앱.
고등학생·대학생이 처음 읽어도 따라올 수 있는 난이도로 쓰되,
심화 내용은 **「🔬 더 깊이」 접이식 상자**에 따로 담아 원하는 사람만 열어 보게 했다.

- 화면 **30개** / 이해도 확인 문제 **95개** / 심화 상자 **27개** / 파이썬 예제 상자 **18개** / 그래프 **16개**
- **API 키·인터넷 없이** 모든 계산이 브라우저 안에서 끝난다.
- 개인정보를 한 글자도 받지 않는다 (이름·학번 칸 없음, 서버 없음).

### ⚠️ 자료 출처와 저작권 — 가장 중요한 결정 (2026-08-10)

원본 강의 자료(`D:\project_AI\LLM_RAG_study\파이썬 텍스트 분석과 LLM-RAG 애플리케이션 개발\`,
PDF 18개 + 노트북 14개, 15주차 분량)는 **진도 범위를 참고하는 데만 썼다.**

- 주차 순서를 그대로 따르지 않고 **학습 흐름으로 재구성**했다 (여섯 걸음 + 실습실).
- **화면 문구·비유·예제 문장·데이터·문제·해설을 전부 새로 썼다.**
  원본의 사례명(회사·서비스 이름), 예제 리뷰 데이터, 문장 표현을 옮겨 오지 않았다.
- 등장하는 **푸른숲 도서관 · 별밤 문구점 · 모퉁이 카페**는 이 앱을 위해 지어낸 가상의 이름이다.
- 원본 자료는 저장소에 넣지 않는다. `.gitignore` 에 `*.pdf *.docx *.ipynb` 를 넣어 두었다.

## ② 기능 목록

| 탭 | 화면 | 핵심 |
|---|---|---|
| Ⅰ 글을 다루기 | 배움 지도 / 전처리 공방 / 한국어 쪼개기 / 정규표현식 놀이터 | 규칙을 껐다 켜며 단계별 결과 비교, 형태소 실시간 분석, 패턴 매칭 하이라이트 |
| Ⅱ 숫자로 바꾸기 | 단어 주머니 / TF-IDF 계산기 / 코사인 유사도 / 감성 분류기 / 단어 임베딩 지도 | 문서 편집 시 표 실시간 재계산, 칸 클릭 시 손계산 과정, 벡터 드래그, **실제 학습** |
| Ⅲ 맥락을 읽기 | 신경망 한 조각 / 순서를 기억하기 / Attention 계산기 / 인코더 블록 | XOR 실제 학습·결정경계, LSTM 게이트 값, Q·K·V 4단계 히트맵, 순서 실험 |
| Ⅳ 말을 만들기 | 다음 낱말 예측기 / 토큰과 컨텍스트 창 / 프롬프트 실험실 / 파이프라인 조립 / 기억하는 챗봇 | 낱말 직접 골라 문장 만들기, 토큰 분해, 프롬프트 스위치, 메모리 온·오프 |
| Ⅴ 내 문서로 답하기 | 임베딩 검색 / 문서 자르기 / RAG 5단계 / 검색 품질 올리기 / 근거와 출처 | 뜻지도 위 질문 위치, 청킹 트레이드오프 측정, **LLM 단독 vs RAG 비교**, 손잡이 4종 |
| Ⅵ 스스로 판단하기 | 상태·노드·엣지 / 갈림길과 루프 / 도구를 쓰는 에이전트 / 멀티턴과 멀티에이전트 | 실행 기록 표, 무한루프 안전장치 체험, 도구 호출 왕복, 대화방 분리 |
| Ⅶ 파이썬 실습실 | 코드 실행 / 빈칸 채우기 / 종합 정리 | 설치 없이 실행, 빈칸 채점 + 실행 동시 확인, 마무리 10문항 |

## ③ 현재 개발 상태와 다음 할 일

**2026-08-10 — 전 범위 완성 + 배포 완료.** `npm test` **188가지 통과**, 문법 전수 검사 통과, 빌드 정상.

- 저장소: https://github.com/trmoo/llm-rag-lab
- Pages: **https://trmoo.github.io/llm-rag-lab/** (열림 확인, 배포본 MD5 가 로컬 빌드와 일치)

### 다음에 할 만한 것 (사용자 요청 시)

- 교사용 설치 안내 페이지 (`public/설치안내.html`) — 다른 앱들과 같은 형태
- 화면별 인쇄용 학습지 출력
- 자기 문서를 올려 RAG 를 돌려 보는 화면 (파일 읽기 필요)

---

## 깃허브 배포 (2026-08-10)

- 첫 푸시 한 번으로 **build·deploy 두 잡이 모두 통과**했다. 손으로 만질 것이 없었다.
- **★ 여기서는 `configure-pages@v5` 의 `enablement: true` 가 문제없이 통과했다.**
  푸시 전에 이미 저장소의 Pages 가 켜져 있었기 때문이다(`has_pages = true`).
  `22aiBasics_trmoo`·`physical_ideation` 은 꺼진 상태라 거부됐었다.
  → **빈 저장소를 새로 만들 때는 Settings → Pages → Source 를 「GitHub Actions」 로
  먼저 켜 두면 첫 푸시부터 깔끔하게 배포된다.**
- ⚠️ **「Deploy from a branch」 를 고르면 안 된다.** `dist/` 를 커밋하지 않는 우리 방식에서는
  깃허브가 빌드하지 않은 원본을 그대로 올려, 개발용 `index.html` 이 서비스되고 흰 화면만 뜬다.
- 이 PC 에 `gh` CLI 가 없다. 확인은 REST API 로 한다.
  ```bash
  curl -s https://api.github.com/repos/trmoo/llm-rag-lab/actions/runs?per_page=3   # 성공 여부
  curl -s https://api.github.com/repos/trmoo/llm-rag-lab/actions/runs/<id>/jobs    # 단계별 결과
  ```
  ⚠️ `.../pages` 엔드포인트는 **인증이 필요해 404 가 나온다.** 배포 확인은 실제 주소로 한다.
  ```bash
  curl -sI https://trmoo.github.io/llm-rag-lab/          # 200 이면 열린 것
  ```
- ⚠️ **무인증 GitHub API 는 시간당 60번뿐이다.** 15~20초마다 상태를 물으면 금방 바닥나고,
  그때부터 응답에 `workflow_runs` 가 없어 `KeyError` 가 쏟아진다(멈춘 것처럼 보이지만 한도 문제다).
  **60초 이상 간격으로 묻거나**, 아예 실제 주소의 MD5 를 로컬 `dist/index.html` 과 대조하는 편이 낫다.
  ```bash
  curl -s https://trmoo.github.io/llm-rag-lab/ -o /tmp/d.html
  md5sum < dist/index.html ; md5sum < /tmp/d.html      # 같으면 반영된 것
  ```
- ⚠️ **연달아 푸시하면 앞 실행이 `cancelled` 로 뜬다.** 워크플로에 `cancel-in-progress: true` 를
  넣어 두었기 때문이며 **정상**이다. 깃 이력은 누적되므로 앞 커밋의 코드도 다음 실행의 빌드에 들어간다.
  실제로 「심화 상자」 실행이 취소됐지만 결과물에는 그 내용이 그대로 들어 있었다(MD5 로 확인).
- 러너가 밀리면 `queued` 상태로 5분 넘게 머물기도 한다. 실패가 아니니 기다리면 된다.
- 깃 전역 사용자 설정이 비어 있어 **앱 폴더에서** 넣어 주었다.
  ```bash
  git config user.name trmoo
  git config user.email trmoo@users.noreply.github.com
  ```

---

## 계산 엔진 — 무엇이 진짜이고 무엇이 흉내인가

**앱 안 「배움 지도」 화면과 README 에 같은 표를 실어 두었다. 고칠 때 세 곳을 함께 고칠 것.**

| 화면 | 실제로 하는 일 | 판정 |
|---|---|---|
| TF-IDF·코사인 | 수식 그대로 (`nlp.js`) | ✅ 진짜 |
| 감성 분류기 | 나이브 베이즈·로지스틱 회귀 브라우저 학습 (`nlp.js`) | ✅ 진짜 |
| 단어 임베딩 | Skip-gram + 네거티브 샘플링 (`w2v.js`) | ✅ 진짜 |
| 신경망·Attention | MLP 역전파, Q·K·V·softmax (`nn.js`) | ✅ 진짜 |
| 그래프·에이전트 | 상태·분기·반복·체크포인트 (`graph.js`) | ✅ 진짜 |
| 다음 낱말 예측 | 문장 50개 n-gram + 백오프 (`lm.js`) | ⚠️ 진짜지만 아주 작음 |
| 형태소 분석 | 사전 + 규칙 (`korean.js`) | ⚠️ KoNLPy 와 다를 수 있음 |
| 문장 임베딩 | 뜻 갈래 20개 + 성질 4개 = 24차원 (`embed.js`) | ⚠️ 원리 같고 규모 다름 |
| RAG 답 만들기 | 근거 문장 추출·조립 (`rag.js`) | ❌ 진짜 LLM 아님 |

흉내인 화면에는 **`.honest` 노란 상자**로 그 사실을 다시 적어 두었다. 이 상자를 지우지 말 것.

---

## 만들면서 겪은 것 (다시 손댈 때 참고)

### 감성 분류 데이터를 두 번 썼다

처음에는 「종이가 두꺼워서 좋다」 ↔ 「종이가 얇아서 나쁘다」처럼 **소재를 뒤집은 짝** 25쌍으로 만들었더니
시험 정확도가 **30%** 까지 떨어졌다. 글자 조각이 거의 같고 뒤집는 낱말 하나만 달라서
모형이 외우기만 한 것이다. 소재를 섞고 감정 표현이 여러 문장에 두루 나오도록 **40+40 으로 다시 썼다.**
짝지은 문장은 「모형의 한계」 화면(`TRICKY_REVIEWS`)으로 옮겼다.

- 자료가 80개뿐이라 **나누는 방식에 따라 55%~95% 로 흔들린다.** 이것을 숨기지 않고
  「일곱 번 나눠 보기」 단추로 **분산 자체를 수업 내용으로** 삼았다.
- 기본 설정은 `min_df=3`(문서 3개 이상에 나온 글자 조각만). 평균 74% 쯤 나온다.
- `npm test` 가 「일곱 씨앗 평균 65% 이상」과 「긍정·부정 신호 낱말이 가중치 상·하위에 있을 것」을 지킨다.

### RAG 답이 처음에는 엉뚱한 문장을 골랐다

「책은 **며칠**까지 빌릴 수 있나요?」 ↔ 안내문의 「대출 **기간**은 21일」 —
**낱말이 하나도 안 겹쳐서** 엉뚱한 문장이 뽑혔다. 네 가지를 더해 9/9 로 맞췄다.

1. **질문 넓히기**(`expandQuery`) — 며칠→기간·일, 빌릴→대출, 돈→연체료·요금. 화면에 무엇이 넓혀졌는지 그대로 보여 준다.
2. **1등 조각 크게 우대**(`chunkBonus`) — 검색이 답을 좌우한다는 RAG 의 성질을 그대로 반영.
3. **수를 묻는 질문에 숫자 문장 우선**(`numberBonus`).
4. **근거는 한 조각에서만** — 여러 조각을 섞으면 답이 뒤죽박죽이 된다.

⚠️ **문장 나누기를 `[.!?다요]` 로 하면 안 된다.** 「하루**마다** 이틀씩」의 '다' 에서 잘린다.
마침표·물음표·느낌표에서만 끊는다. 시험이 이것을 지킨다.

### 한국어 분석기에서 두 번 막혔다

- **「종이가」 → 「종 + 이 + 가」** — 조사를 욕심내어 두 번 뗀 탓. 조사를 **적게 뗀 갈래부터**
  명사 사전에 걸리는지 보도록 고쳤다(`josaCandidates`).
- **「연체하면」이 통째로 한 낱말** — 문서 속 「연체」와 이어지지 않아 검색이 실패했다.
  **「명사 + 하다」 꼴**(`HADA_ENDINGS`)을 따로 처리한다. 검색이 깨지는 흔한 원인이라 시험에 넣었다.

### 미니 파이썬에 더한 것

`object-oriented/src/pymini.js` 를 가져와 텍스트 분석 수업에 필요한 것을 더했다.

- 내장 함수 `sorted enumerate zip set any all reversed dict tuple`
- 모듈 `re` (sub·findall·split·search·match) / `collections.Counter`(→`most_common`) / `korean`
- **`r'...'` 날 문자열** — 정규표현식에 꼭 필요한데 없었다
- **리스트 컴프리헨션** — 수업에서 계속 쓰는데 아예 없었다(`Comp` 로 검색하면 `Compare` 만 걸려 착각하기 쉽다)
- **슬라이스 `글[2:5]`** — 청킹 예제에서 필요했다
- 딕셔너리를 `print` 하면 열쇠가 `undefined` 로 찍히던 버그 수정(원시값 키를 파이썬 값으로 되감지 않았다)

⚠️ **`set()` 은 「중복을 없앤 리스트」를 돌려준다.** 진짜 집합이 아니다.
수업에서 쓰는 자리(어휘 만들기)에서는 결과가 같아서 이렇게 두었고, 화면에 밝혀 두었다.
`lambda`·`with`·`try/except`·파일 입출력은 지원하지 않는다.

### ⚠️ 화면 요소를 문자열과 `+` 로 이어 붙이면 안 된다

`mono()`·`b()` 같은 도우미는 글자가 아니라 **화면 요소**를 돌려준다.
문자열과 `+` 로 이으면 자바스크립트가 요소를 글자로 바꾸면서 `[object HTMLElement]` 가 그대로 찍힌다.

```js
mono('[^가-힣]') + ' = 한글이 아닌 것'          // ❌ [object HTMLElement] = 한글이 아닌 것
h('span', mono('[^가-힣]'), ' = 한글이 아닌 것') // ✅ 나란히 놓는다
```

정규표현식 놀이터의 「자주 쓰는 조각」 표 네 줄이 이 꼴이었고, **오류가 나지 않아 조용히 배포됐다.**
그래서 `ui.js` 의 `h()` 안 `append()` 에 **감지 장치**를 넣었다 —
`[object ` 가 든 글자가 화면에 붙으려 하면 무엇을 어떻게 고칠지까지 적어 큰 오류로 알린다.
`mono('[' + a + ']')` 처럼 **괄호 안에서** 잇는 것은 문제가 없다(글자끼리 잇고 나서 넘기므로).

### ⚠️ 같은 화면으로 보내면 아무 일도 일어나지 않는다

`location.hash` 가 그대로면 `hashchange` 가 일어나지 않아 단추가 먹통이 된다.
배움 지도(탭 ①의 첫 화면)에서 ① 「가기」가 자기 자신을 가리켜 이 일이 실제로 벌어졌다.
`main.js` 의 `go()` 가 **같은 화면이면 직접 다시 그리도록** 고쳤다.
그래도 「지도에서 지도로」는 뜻이 없으니, ① 카드는 전처리 공방으로 보낸다(`stepCard` 의 마지막 인자).

### 그 밖

- **`beginScreen()`** — 탭을 옮기면 화면은 지워지지만 `resize` 리스너와 타이머는 살아남아 쌓인다.
  화면을 그리기 직전에 전부 걷어 낸다. **탭 파일에서 `window.addEventListener`·`setInterval` 을 직접 쓰면 안 되고,
  `npm test` 가 이것을 검사한다.**
- **`choicePicker` vs `pillGroup`** — 채점이 붙는 고르기는 반드시 `choicePicker`.
  `pillGroup` 은 첫 항목이 기본 선택이라 안 고르고 [확인] 을 눌러도 골라진 것으로 처리된다.
- 그래프는 `src/lib/chart.js` 로 직접 그린다. **외부 라이브러리 0개** (교실 인터넷 대비).

---

## 시험

```bash
npm test              # 188가지
npm run check:syntax  # 소스 25개 전수 문법 검사
```

| 파일 | 지키는 것 |
|---|---|
| `test/nlp.test.mjs` | TF-IDF 수치(IDF 노트 0.9163 · 배송 0.0 · TF-IDF 0.4581), 형태소·조사, 분류기 성능, 정확도의 함정 |
| `test/rag.test.mjs` | **안내문 사실 9가지를 모두 맞히는가**, 답 없는 질문 3가지에 「모른다」고 하는가, 규칙을 끄면 지어내는가, 청킹·검색·프롬프트 |
| `test/python.test.mjs` | 실습실 예제·빈칸 문제가 **실제로 돌아가고 적어 둔 출력과 같은가** |
| `test/app.test.mjs` | 그래프 엔진, 언어 모델, 신경망, Word2Vec, **퀴즈 자료 무결성**(열쇠 중복·해설 누락·보기 번호), 화면 수명 관리, 저작권 표시 |

⚠️ **`npm test` 는 탭 파일(DOM)을 불러오지 않는다.** 그래서 탭 파일 문법 오류는 시험을 통과하고
빌드에서야 터진다. `npm run check:syntax` 를 함께 돌리는 이유이고, 배포 워크플로에도 넣어 두었다.

---

## 저작권 표시 일곱 곳

프로젝트 방침(`D:\project_AI\CLAUDE.md`)대로 **오픈소스 라이선스를 붙이지 않았다.**

① `LICENSE` ② `README.md` ③ `package.json` 의 `"license": "UNLICENSED"`
④ `index.html` 주석 ⑤ 소스 파일마다 `/*!` 머리 주석 ⑥ **화면 아래 푸터** ⑦ `vite.config.js` 의 배너

- `LICENSE` 에 **「설명·예시·데이터의 출처」** 절과 **「라이브러리 이름에 관하여」** 절을 따로 넣었다.
  라이브러리 이름은 상표이고, 이 앱은 그것을 포함·재배포하지 않는다는 것을 밝혀 두었다.
- 빌드 후 확인: `grep -c 티쳐무 dist/index.html` 이 **0이 아니어야** 한다 (모듈마다 배너가 남아 15줄쯤 나온다).
- ⚠️ 확인할 때 **파워셸 `Get-Content` 를 쓰면 안 된다.** BOM 없는 UTF-8 을 CP949 로 읽어
  한글이 깨져 「0회」라는 거짓 결과가 나온다. `grep` 이나 파이썬으로 확인한다.

## 빌드

```bash
npm run build   # → dist/index.html 한 파일 (약 380KB, 더블클릭 실행)
```

`vite-plugin-singlefile` 로 자바스크립트·CSS·데이터를 전부 한 파일에 넣는다.
`base: './'` 라서 GitHub Pages 하위 경로에서도 자원이 열린다.
